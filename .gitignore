# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# NeoGradle dev run
run/
runs/
logs/

# Vendored compile-time jars.
#
# The build compiles against the exact jars the pack runs, taken straight from the instance --
# see the Building section of the README for which ones and why. They are third-party mods and
# 27MB besides, so they are not tracked. Copy them in from the instance's mods/ folder before
# building.
libs/

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
bin/
